<div class="container">
<div class="d-flex align-itmes-center mt-4 mb-4">
  <div class="ml-auto boards__linkBox">
    <% if @current_user.present? %>
    <% if @current_user.id == @company.user_id %>
    <%= link_to '編集', edit_company_path(@company), class: 'btn btn-outline-dark' %>
    <% end %>
    <% end %>
  </div>
</div>

<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>

<%= render @company %>

</div>
